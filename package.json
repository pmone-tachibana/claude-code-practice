{
  "name": "claude-code-practice",
  "private": true,
  "type": "module",
  "description": "Dev tooling for the TODO app (the app itself has no build step — see CLAUDE.md).",
  "scripts": {
    "crawl": "node scripts/crawl.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
